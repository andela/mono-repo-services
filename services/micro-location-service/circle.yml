machine:
  node:
    version: 5.8.0
  environment:
    NODE_ENV: test
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test
    COVERALLS_SERVICE_NAME: circle-ci
    COVERALLS_REPO_TOKEN: blablabla
dependencies:
  pre:
    - make dependencies

test:
  post:
    - make test
